# Where uvicorn is serving backend/app/main.py.
# Used by next.config.ts to proxy /api/* and /health, keeping the browser
# same-origin so the backend needs no CORS middleware.
BACKEND_URL=http://127.0.0.1:8000
